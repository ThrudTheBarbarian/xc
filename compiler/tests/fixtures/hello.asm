;; Generated by xtc --emit-asm-from-ir
;; Source: tests/fixtures/hello.xt

    .org $2000


    LDA #<stack_low
    STA $82
    LDA #>stack_low
    STA $82 + 1

    LDA #<heap_top
    STA $86
    LDA #>heap_top
    STA $86 + 1


main_entry:
    JSR _main
    JMP ($000A)   ; exit through DOSVEC

.module hello

    ; === function main ===
_main:
    ; var x =
    ; const 42
    LDA #$2A
    STA $81
    STA _x
    ; return
    RTS
    ; === end main ===

    ; === Global variables ===
    _x:
        .space 1

    ; === Runtime routines ===


; ============================================================
; BEGIN xtc-manifest v1
; module.name                      = hello
; module.source                    = hello.xt
; module.compiler                  = xtc 0.12 (IR route)
; module.target                    = atari/xl
;
; ─ Per-function call-graph attributes ─────────────────────
; function._main.is_main       = true
;
; ─ Per-class inheritance + vtable info ────────────────────
; class.Object.class_id  = 1
; END xtc-manifest
; ============================================================

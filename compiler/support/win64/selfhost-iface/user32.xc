// user32 — bundled Win32 interface stub for the self-hosted win64 path (no mingw).
// The load-bearing Win32 entry points are declared in Platform.xc; this file
// exists so #import <user32> resolves without the mingw sysroot. Extend as needed.
